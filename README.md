# air_quality_dwh
A Data Warehouse project for monitoring air quality. It features an automated Python and Apache Airflow ETL pipeline that extracts live API data, processes it (including delta loads and SCD Type 2), and loads it into a SQL Server Star Schema. The project includes interactive BI dashboards built with Dash and Plotly.
